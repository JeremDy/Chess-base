{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('profileEdit',{ 'id':app.user.id}), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}
    <div>
         <a class="btn btn-primary" href="{{ path('profileShow',{ 'id':app.user.id}) }}">Annuler</a>
        <input class="btn btn-dark" type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
{{ form_end(form) }}
