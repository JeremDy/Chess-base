{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('profileEdit',{ 'id':app.user.id}), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}
    <div>
         <a class="badge badge-primary" href="{{ path('profileShow',{ 'id':app.user.id}) }}">Annuler</a>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
{{ form_end(form) }}
